package views

import "erosync/views/components"

templ RegisterContent() {
	<div class="grid grid-cols-2 min-h-screen">
		<div class="bg-primary"></div>
		<div class="flex flex-col items-center justify-center bg-gray-100">
			<form method="post" class="w-125">
				<h3 class="text-3xl font-bold">Register</h3>
				<p class="text-muted-foreground">Enter your detials to access the portal</p>
				<div class="space-y-4 my-6">
					@components.Input("name", "text", "Full Name", "")
					@components.Input("email", "email", "Email Address", "")
					@components.Input("password", "password", "Password", "")
				</div>
				<button type="submit" class="h-12 w-full bg-primary text-white uppercase font-medium">Register</button>
			</form>
			// <Link class="text-primary hover:underline" to="/forgot-password">
			// Forgot Password
			// </Link>
		</div>
	</div>
}

templ Register() {
	@components.Base("Register", RegisterContent())
}
