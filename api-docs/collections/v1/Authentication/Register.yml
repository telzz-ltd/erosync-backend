info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/register"
  body:
    type: json
    data: '{"name": "usman baba", "email": "baba@example.com", "password": "pass1234"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
