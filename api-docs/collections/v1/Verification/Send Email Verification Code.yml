info:
  name: Send Email Verification Code
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/verification/email/send-otp"
  auth:
    type: bearer
    token: "{{refreshToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
